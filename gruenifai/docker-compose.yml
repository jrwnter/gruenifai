version: '3'

services:
  database:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: 'postgres'
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'postgres'
    ports:
      - 5432:5432
  backend:
    build:
      context: backend
      args:
        - http_proxy
        - https_proxy
    links:
      - database
    expose:
      - 8897
  gui_backend:
    build:
      context: gui/server
      args:
        - http_proxy
        - https_proxy
    links:
      - database
    expose:
      - 5000
  gui_client:
    build:
      context: gui/client
      args:
        - http_proxy
        - https_proxy
    links:
      - gui_backend
    expose:
      - 80
    ports:
      - 8777:80